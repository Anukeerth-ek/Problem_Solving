// 2️⃣ Find Missing Number
// Given nums = [0..n] missing one number → return missing one.

// const findMissingNumber = (number) => {
    
//     const length = number.length;

//     const expectedSum = length * (length + 1) / 2;

//     let sum = 0;

//     for (let num of number) {
//         sum += num;
//     }

//     return expectedSum - sum;

// }

// console.log(findMissingNumber([3, 0, 1]))
// console.log(findMissingNumber([9, 6, 4, 2, 3, 5, 7, 0, 1]))
